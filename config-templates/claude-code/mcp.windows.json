{
  "mcpServers": {
    "memcord": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "--directory",
        "{{MEMCORD_PATH}}",
        "run",
        "memcord"
      ],
      "env": {
        "PYTHONPATH": "{{MEMCORD_PATH}}/src",
        "UV_PROJECT_ENVIRONMENT": "{{MEMCORD_PATH}}/.venv",
        "MEMCORD_MEMORY_DIR": "",
        "MEMCORD_SHARED_DIR": ""
      }
    },
    "memcord-advanced": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "--directory",
        "{{MEMCORD_PATH}}",
        "run",
        "memcord"
      ],
      "env": {
        "PYTHONPATH": "{{MEMCORD_PATH}}/src",
        "UV_PROJECT_ENVIRONMENT": "{{MEMCORD_PATH}}/.venv",
        "MEMCORD_ENABLE_ADVANCED": "true",
        "MEMCORD_MEMORY_DIR": "",
        "MEMCORD_SHARED_DIR": ""
      }
    }
  }
}
