---
description: Remove .memcord binding from a project directory
---

Remove .memcord binding from project: $ARGUMENTS

Use the memcord_unbind tool to remove the .memcord file from a project directory, unbinding it from its memory slot.
