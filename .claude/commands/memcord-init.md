---
description: Initialize a project directory with a memory slot
---

Initialize project directory with memory slot: $ARGUMENTS

Use the memcord_init tool to associate a project directory with a memory slot by creating a .memcord file. If no slot name is provided, uses the directory name.
